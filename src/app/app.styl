body {
  font-family: sans-serif;
}

core-header-panel {
  background: white;
  /* height: 100% fails here on some browsers */
  height: 100vh;
  width: 100vw;
}

core-toolbar {
  background-color: #03A9F4;
}

p {
  margin: 20px;
  padding: 80px 20px;
  border-radius: 20px;
  background-color: #eee;
}

#sad-graphs
  paper-button.start-job
    background-color: #03a9f4;
    margin-left: 40%;
    &.active
      background-color: darkgrey;
